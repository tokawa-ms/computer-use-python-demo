# Azure OpenAI endpoint settings
# Prefer AZURE_OPENAI_ENDPOINT. Example:
# AZURE_OPENAI_ENDPOINT=https://<your-resource-name>.openai.azure.com
AZURE_OPENAI_ENDPOINT=

# Alternative forms (use one of these instead of AZURE_OPENAI_ENDPOINT if you prefer)
# AZURE_OPENAI_RESOURCE_NAME=
# AZURE_OPENAI_BASE_URL=https://<your-resource-name>.openai.azure.com/openai/v1/

# Azure OpenAI *deployment names* (NOT model IDs)
# These are the names you configured when deploying models in Azure AI Foundry/Azure OpenAI.
AZURE_OPENAI_MODEL_COMPUTER_USE=
AZURE_OPENAI_MODEL_SUMMARY=
AZURE_OPENAI_MODEL_CONFIRM=

# Task parameters (you can also pass via CLI flags)
TARGET_APP=Microsoft Teams
TARGET_RECIPIENT=
TARGET_MESSAGE=

# Optional toggles (edit in code if you need more control)
# AUTO_CONFIRM=true/false is not wired as env var in code currently.
